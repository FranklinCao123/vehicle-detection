model:
  name: YOLOv8s
  weights: models/yolov8s.pt

detection:
  conf_threshold: 0.5
  imgsz: 1280

preprocessing:
  enabled: true
  color_correction: null   # 新增：开启去灰
  clahe_clip_limit: 0.8
  sharpen: true

paths:
  input_dir: data
  input_image: ""
  output_dir: results